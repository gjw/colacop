# macOS metadata
.DS_Store

# bv (beads viewer) local config and caches
.bv/

# Claude Code local telemetry
.claude/tracking/

# Node / build
node_modules/
dist/

# Env
.env

# Runtime data (keep dirs via .gitkeep)
data/incoming/*
data/processing/*
data/processed/*
data/failed/*
!data/incoming/.gitkeep
!data/processing/.gitkeep
!data/processed/.gitkeep
!data/failed/.gitkeep

# Raw fixture inputs (workshop material — processed composites under
# fixtures/labels/ and fixtures/applications/ are the committed deliverables)
fixtures/raw/*
!fixtures/raw/README.md
