{"id":"cc-1c5","title":"First-class reviewer experience on live URL: pre-load + downloadable fixtures + post-upload navigation","description":"Reviewer experience on the live deployed URL needs to be first-class. The reviewer arrives at https://colacop.foramerica.dev cold; they must see something interesting on first load AND have an obvious way to exercise the workflow themselves end-to-end with their own latency observation. This bead now covers the unified reviewer-onboarding affordance.\n\nSix things in scope:\n\n1. Pre-load the 6 demo fixture pairs (cointreau, agave, rumble, fireball, shinok, rumple) into the deployed instance at deploy time so the queue is non-empty on first load.\n2. Downloadable links to each of the 6 fixture pairs, exposed via HTTP from a fixtures-listing endpoint or a static mount, so a reviewer can grab a pair, modify the JSON, and upload the modified version to test specific scenarios.\n3. Visible JSON schema on or beside the upload form (a small <pre> example block showing the expected shape of an application JSON), so the reviewer doesn't have to read source code to know what's accepted.\n4. Format help on the upload form: one-line hint that 'image: front+back composite of a label; JSON: producer's claimed values; either alone is OK'.\n5. Post-upload navigation: after a successful upload, navigate the reviewer directly to the new job's detail view rather than leaving them on the queue. The 5-second perceived-latency story (F-010) lives in the user watching the verdicts populate in front of them.\n6. A subtle processing indicator in the detail view while lifecycle='processing' (a spinner or pulse), so the reviewer sees the system working rather than waiting.\n\nThe reset-to-demo-state secondary affordance is filed separately (see cc-rst); build only with Sunday afternoon slack.","design":"BACKEND:\n- Static-serve mount or dedicated route for fixture downloads. Probably /api/fixtures/<stem>.{jpg,json} with a content-disposition header so the browser actually downloads rather than displays. Restrict to known fixture stems to avoid serving arbitrary repo content.\n- Seed routine: a script (scripts/seed-fixtures.ts or npm run seed:fixtures) that copies fixtures/labels/*.{jpg,png,webp} and fixtures/applications/*.json into data/incoming/. Idempotent — running it twice doesn't duplicate. Run from the deploy pipeline (cc-ckg) AFTER migrations succeed.\n- Upload endpoint already exists; no backend change for upload itself.\n- POST /api/jobs/upload should return the new job_id (not just the stem) so the UI can navigate.\n\nUI:\n- src/web/App.tsx UploadForm: add format-help text and a JSON example block. After successful upload, set the selected job to the new job_id and let the existing detail-view polling render it.\n- New 'sample submissions' panel above or beside the upload form: lists the 6 fixtures with download links for each pair (image + JSON).\n- JobDetailView: while job.lifecycle === 'processing', show a small inline spinner alongside the lifecycle badge. Existing 2-second polling already drives the transition; the spinner just makes it feel responsive.\n\nOUT OF SCOPE: validation of uploaded JSON shape on the client (let the worker reject malformed JSON via the 'failed' lifecycle path); per-fixture preview thumbnails in the sample-submissions panel; rate-limiting (uploads cost ~/bin/zsh.01 in Gemini per call; even a malicious reviewer pinging the form 200 times costs  — accept).\n\nDEPS: cc-7w9 (real Gemini wired so the seed produces real findings), cc-ckg (so there is a deployed instance to seed).","acceptance_criteria":"1. On a fresh deploy, https://colacop.foramerica.dev shows the 6 demo bottle jobs already processed and ready for review on first page load.\n2. The page includes a visible 'sample submissions' panel with download links for each of the 6 fixture pairs (image + JSON).\n3. The upload form has a one-line format hint and a small JSON example block adjacent.\n4. Uploading a paired or label-only submission returns the reviewer to the new job's detail view (not the queue), with a visible processing indicator while lifecycle='processing'.\n5. The detail view shows a spinner or equivalent processing affordance during lifecycle='processing'; the spinner disappears when lifecycle transitions to 'processed' or 'awaiting_application'.\n6. The seed script is idempotent (running it twice doesn't duplicate fixture jobs).\n7. The seed script runs as part of the deploy pipeline in cc-ckg, not a manual SSH step.\n8. typecheck/lint/test pass.","status":"open","priority":0,"issue_type":"task","created_at":"2026-05-01T23:52:44.251528Z","created_by":"gjw","updated_at":"2026-05-03T19:16:05.560613Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["deployment","mvp"],"dependencies":[{"issue_id":"cc-1c5","depends_on_id":"cc-7w9","type":"blocks","created_at":"2026-05-01T23:54:10.309389Z","created_by":"gjw","metadata":"{}","thread_id":""},{"issue_id":"cc-1c5","depends_on_id":"cc-ckg","type":"blocks","created_at":"2026-05-01T23:54:10.386004Z","created_by":"gjw","metadata":"{}","thread_id":""}],"comments":[{"id":1,"issue_id":"cc-1c5","author":"gjw","text":"## Path & file-lifecycle research from cc-ckg deploy session (2026-05-03)\n\nTracing through `routes/jobs.ts`, `routes/files.ts`, `routes/upload.ts`, `worker/watcher.ts`, and the DB schema. Recording here for reviewer-experience design.\n\n### Path map (relative to project root)\n\n| Concept | Path | Source |\n|---|---|---|\n| Fixture images (committed) | `fixtures/labels/*.jpg` (agave, cointreau, fireball, rumble, rumple, shinok) | git |\n| Fixture application JSON (committed) | `fixtures/applications/*.json` (matching set) | git |\n| Watched dir | `data/incoming/` | `worker/watcher.ts:69` (chokidar) |\n| Upload destination | `data/incoming/` (same as watched) | `routes/upload.ts:43`, multer renames to `upload-{ts}-{hex}.{ext}` |\n| Image displayed in UI | `data/incoming/{basename(image_path)}` served via `/api/files/{basename}` | `routes/jobs.ts:10-13` + `routes/files.ts:30` |\n| Application JSON displayed in UI | `data/incoming/{stem}.json` — **read live from disk on every detail-view request, not stored in DB** | `routes/jobs.ts:76-79` (`fs.readFile` + Zod parse) |\n\n### What's actually in the DB\n\n`jobs` row: `id`, `stem`, `image_path` (text/abs path), `application_path` (text/abs path), `lifecycle`, `failure_reason`, timestamps. Plus `layer1_results` and `layer2_results` rows. **No blob, no JSON content** for the image or the application data.\n\n### Three implications for the reviewer experience\n\n1. **`data/processing/`, `data/processed/`, `data/failed/` are scaffolded but unused.** The `lifecycle` column moves through states (`pending` → `queued` → `processing` → `processed`) in the DB only — no code moves files between those directories. Files stay in `data/incoming/` indefinitely. After the agave smoke test, `agave.jpg`/`agave.json` were still in `data/incoming/` post-success. Re-dropping the same fixture pair is a Chokidar no-op (already-known file → no `add` event).\n\n2. **Image URL is built from basename only** (`/api/files/{basename(image_path)}`), but the files router always resolves against `incomingDir`. If a file ever moved to a different directory but the DB row still pointed at it, the served image would 404 — basename + `incomingDir` wouldn't find it. The system happens to work because nothing moves files. Coupled assumption.\n\n3. **Application JSON is fragile to filesystem changes.** Delete `data/incoming/{stem}.json` after processing → detail view returns `application: null` (the `try/catch` in `loadApplication` swallows the read error). The verdicts (which ARE in DB) still render, but the human-readable application data block disappears.\n\n### Reviewer-experience design considerations (for cc-1c5 scope)\n\n- **Pre-load fixtures**: dropping copies of `fixtures/labels/*.jpg` + `fixtures/applications/*.json` into `data/incoming/` on deploy gives reviewers an immediate populated jobs list without an upload step.\n- **Downloadable fixtures**: surface the committed fixtures as downloadable links so reviewers can save them locally and then drag-drop them into the upload UI to exercise that path. Two ways to do this:\n  - Static-serve `fixtures/labels/` and `fixtures/applications/` via nginx (read-only, separate location block) — cheapest.\n  - Add an Express route that lists fixtures and streams them — gives more control but more code.\n- **Post-upload navigation**: today the upload endpoint returns `{stem, written}` but the UI doesn't auto-route to the new job's detail page. Reviewers have to wait, refresh the list, then click. After upload, the UI should navigate to `/jobs/{id}` once the job appears (poll `/api/jobs?stem=<stem>` until the row shows up, then route).\n- **File-lifecycle gap is out of scope here** unless reviewer-experience design wants to expose lifecycle visually — e.g., showing where a file currently lives. Probably not worth it for a 3-day prototype; recording the gap for awareness.\n","created_at":"2026-05-03T19:16:05Z"}]}
{"id":"cc-4fk","title":"Pick deployment target for live URL","description":"Assignment requires a live deployed URL. ARCHITECTURE.md presumes a VPS with nginx + PM2 + Docker (Postgres). No specific provider chosen. Pick one (DigitalOcean droplet, Hetzner, Fly.io equivalent, etc.), provision early, and prove a deploy works before the end. Update ARCHITECTURE.md with the choice and capture the deploy steps in README.md.","notes":"Decision: Linode VPS linode43393 (Linode 16 GB, US Dallas TX, 45.33.122.158). URL: https://colacop.foramerica.dev (wildcard *.foramerica.dev Let's Encrypt cert already issued, no per-subdomain TLS work needed). Existing nginx and PM2 already on the box; Postgres runs via docker compose only (no native install). Deploy mechanism: manual ssh + git pull + npm ci + npm run migrate + npm run build + pm2 reload — intentionally not CI/CD-driven for 3-day iteration speed. ARCHITECTURE.md > Deployment Shape > Live deployment target subsection records the host details, deploy steps, and coordination notes. README.md bootstrapped with reviewer-facing live URL, local-run instructions, deploy story, and assumptions/limitations.","status":"closed","priority":0,"issue_type":"decision","created_at":"2026-04-29T18:37:21.415738Z","created_by":"gjw","updated_at":"2026-04-29T22:19:11.001544Z","closed_at":"2026-04-29T22:19:11.001175Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["decision","deployment"]}
{"id":"cc-55c","title":"Layer 2 detail: show extracted-vs-application values side-by-side","description":"UC-001 step 4 explicitly asks the system to show, per comparable field, the value extracted from the label and the value supplied in the application, side by side. Today JobDetailView shows only the verdict and a reason-string that includes both values inline. Restructure the Layer 2 section as columns: field name | extracted value | application value | verdict | message. Same restructure or a simpler version may suit Layer 1 if useful.","design":"src/web/App.tsx JobDetailView:\n- Layer 2 section restructures from row-per-field-with-message to a table: columns 'Field | Extracted | Application | Verdict | Reason'.\n- Each row's 'Extracted' shows the extracted ExtractedField value (need API response to include it; today the API returns Layer 2 results without the underlying extracted values, so the API needs to include extracted_fields alongside).\n- Layer 1 section optionally gets the same treatment but is less critical — Layer 1 is label-vs-regulation and the 'application' column doesn't apply.\n\nAPI:\n- GET /api/jobs/:id should include the ExtractedFields produced by the worker (currently the worker persists Layer 1 + Layer 2 results but doesn't necessarily persist the raw ExtractedFields; verify and add an extracted_fields table or JSON column on jobs if needed).\n- GET /api/jobs/:id should include the parsed ApplicationData (currently includes application_path; the UI would have to fetch and parse, which is fine, but cleaner to include the parsed JSON in the response).\n\nDEPS: cc-7w9 (real data makes this view meaningful; with stub data every row is the same).","acceptance_criteria":"1. The Layer 2 section in JobDetailView shows columns 'Field | Extracted | Application | Verdict | Reason' with values populated.\n2. When extracted is present and application is missing, the cell shows 'needs application data' rather than blank.\n3. typecheck/lint/test pass.","notes":"Demoted from P1 to P2 by Tower 2026-05-02: cc-o1k landing extracted_value (L1) and extracted_value+application_value (L2) under each finding row largely satisfies the 'agent can read both sides at a glance' need from UC-001 step 4. cc-55c is now table-layout polish on top — cleaner column-based presentation with sortable/filterable rows. Build only with Sunday afternoon slack; otherwise defer.","status":"open","priority":2,"issue_type":"task","created_at":"2026-05-01T23:52:47.703246Z","created_by":"gjw","updated_at":"2026-05-03T02:57:13.332804Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-55c","depends_on_id":"cc-7w9","type":"blocks","created_at":"2026-05-01T23:54:19.521268Z","created_by":"gjw","metadata":"{}","thread_id":""},{"issue_id":"cc-55c","depends_on_id":"cc-vho","type":"blocks","created_at":"2026-05-01T23:54:19.579006Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-5ea","title":"TTB regulatory mini-pass: ground warning-text + mandatory-fields rules","description":"Per docs/handoff.md, this is the gating step before use cases. Add 1-2 source files under discovery/sources/ (S-006, S-007) capturing only the regulatory facts the prototype needs: mandatory label fields, government warning exact text, warning capitalization/formatting requirements, alcohol-content display basics. Extract those into discovery/facts.yaml as F-044+. Update REQ-004 and REQ-005 supported_by to cite the regulatory facts, not just Jenny's interview. Keep targeted — do NOT model alcohol-labeling law in full.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-04-29T18:36:51.299065Z","created_by":"gjw","updated_at":"2026-04-29T22:23:56.210757Z","closed_at":"2026-04-29T22:23:56.210317Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["blocking","discovery"]}
{"id":"cc-64v","title":"Reviewer-facing README polish: setup, run, deploy URL, approach summary","description":"Per assignment deliverables: README needs (a) setup + run instructions for local docker-compose-up + npm run flow, (b) link to live deployed URL, (c) brief approach/tools/assumptions documentation, (d) explicit trade-offs and limitations. Currently README is just a discovery overview. Touch this only after vertical slice + deployment exist.","status":"open","priority":1,"issue_type":"task","created_at":"2026-04-29T18:37:21.512143Z","created_by":"gjw","updated_at":"2026-04-29T18:37:28.537823Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["deliverable","docs"],"dependencies":[{"issue_id":"cc-64v","depends_on_id":"cc-4fk","type":"blocks","created_at":"2026-04-29T18:37:28.537676Z","created_by":"gjw","metadata":"{}","thread_id":""},{"issue_id":"cc-64v","depends_on_id":"cc-xog","type":"blocks","created_at":"2026-04-29T18:37:28.487082Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-7qe","title":"Pick first model/OCR provider implementation","description":"ARCHITECTURE.md isolates the provider behind analyzeLabel/verifyLabel. First concrete impl is undecided. Candidates: Anthropic vision (note F-042 supply-chain risk), OpenAI vision, local Tesseract+small LLM, hosted OCR + LLM split. Decide based on: TTB network restrictions (F-025), 5-second perceived-latency target, prototype budget. Document choice in ARCHITECTURE.md and add provider impl behind the interface.","notes":"Decision: Google Gemini 3.1 Pro Preview (released 2026-02-19), via the Gemini API directly. Rationale recorded in ARCHITECTURE.md > Model Provider Boundary. Anthropic excluded on procurement grounds (F-042). xAI excluded — grok-vision still beta. OpenAI GPT-5.5 viable fallback but ~17% behind on MMMU-Pro and 2.5x more expensive. Provider interface narrowed: analyzeLabel on provider, verifyLabel is domain logic (core/verification.ts) operating on ExtractedFields. Verification is two-layer per F-045/REQ-011: Layer 1 well-formedness (deterministic), Layer 2 comparison (hybrid: deterministic for warning/ABV/contents, LLM-assisted via the same Gemini provider for brand/class). Production swap path: Vertex AI Government / Azure OpenAI / Bedrock per TTB procurement catalog.","status":"closed","priority":1,"issue_type":"decision","created_at":"2026-04-29T18:37:21.371688Z","created_by":"gjw","updated_at":"2026-04-29T21:28:06.524526Z","closed_at":"2026-04-29T21:28:06.524114Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["blocked-by-context","decision"]}
{"id":"cc-7v3","title":"Fireball Layer 1 alcoholContent: false-fail despite valid 'Alcohol 16.5% by volume' on label","description":"Live processing of fixtures/labels/fireball.jpg (the front+back composite) yields layer1_results.alcoholContent.verdict='fail' with message 'Alcohol content does not conform to 27 CFR 4.36 format'. The label DOES have a compliant alcohol statement on the back panel — Chair confirms 'Alcohol 16.5% by volume' is printed there as VERTICAL text, rotated 90 degrees from the front panel orientation.\n\nConflicting evidence:\n- Offline capture during cc-9cx (fixtures/raw/fireball.extraction.json) returned alcoholContent.value='ALC. 16.5% BY VOL.' which the post-cc-9cx ABV_FORMAT regex accepts cleanly (parses as 16.5).\n- Live DB after the post-direnv-fix re-run (2026-05-02) shows verdict='fail', extraction_confidence='hi'. So Gemini DID extract something with high confidence — but it didn't parse.\n- Schema does NOT persist the extracted value (this is exactly the gap cc-o1k addresses), so we can't inspect what was actually returned this run without re-running Gemini.\n\nHypotheses to check (cheapest first):\n1. Non-determinism: Gemini returned a different alcoholContent string this run than in the offline capture. Re-run scripts/capture-extractions.ts (cost: ~/bin/zsh.06 total) and compare against offline snapshot. If the new extraction parses, the bug is the offline snapshot was atypical and the regex is fine; resolution may be: tighten prompt to force consistent format.\n2. Vertical text extraction: composite has the back panel rotated 90deg; Gemini may misread vertical text and produce something like '%5.61 LOV YB LCA' or partial fragments. Visual confirmation by re-rendering.\n3. Composite orientation: fixtures/raw/README.md notes the rumble composite needs '-rotate -90' but does NOT note the same for fireball. If fireball's back panel was stitched without rotation correction, the back-panel text may be unreadable.\n\nDEPENDENCY: this is much easier to diagnose after cc-o1k lands (extracted_value visible in UI/DB). Until then: ad-hoc Gemini re-run or temporary console.log in worker/process.ts.\n\nDemo concern: fireball is one of 6 demo bottles; a visible false-fail on Layer 1 looks worse than any other failure mode because the regulation it cites is satisfied on the actual label. Aim to resolve before any review demo.\n\nAcceptance criteria:\n1. Root cause identified — one of {Gemini returned malformed string, Gemini missed vertical text, composite-orientation issue, regex still has a gap} — and named explicitly in close reason.\n2. Fireball's Layer 1 alcoholContent verdict shows 'pass' on the next live re-process.\n3. If the cause is a Gemini-prompt or composite-orientation fix, the fix does not regress the 5 other bottles' extractions (re-run scripts/capture-extractions.ts and diff).","notes":"Diagnosis sequence updated: do this AFTER cc-o1k lands so the extracted value is visible in the UI; root-causing fireball without seeing what Gemini returned is debugging blind. Top hypothesis (Tower's bet 2026-05-02): vertical-text on the back panel — fireball's composite stitches the back without rotation, and Gemini garbles the vertical 'Alcohol 16.5% by volume'. Cheapest fix is to re-stitch fireball with the back panel rotated to match the front, then re-run.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-05-03T02:48:51.534546Z","created_by":"gjw","updated_at":"2026-05-03T02:57:09.581968Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-7v3","depends_on_id":"cc-o1k","type":"blocks","created_at":"2026-05-03T02:57:09.517468Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-7w9","title":"Wire real Gemini provider; replace stub in worker startup","description":"Stub LabelProvider is the proximate blocker on testing the prototype with real labels — it returns identical canned data for any unknown stem so all three real bottles in fixtures/labels/ currently produce identical findings (good-merlot defaults). Wire a GeminiLabelProvider implementing LabelProvider.analyzeLabel(imagePath) by calling Gemini's vision API with structured-JSON output matching the existing ExtractedFields shape, and select the provider in worker startup based on env var presence (GEMINI_API_KEY). While here, extend ExtractedFields with countryOfOrigin so Layer 2 can actually compare it.","design":"FILES:\n- NEW: src/core/providers/gemini.ts implementing LabelProvider.\n- EDIT: src/core/schemas.ts — add countryOfOrigin to ExtractedFields (also as ExtractedField with confidence) + applicationDataSchema already has it.\n- EDIT: src/core/verification.ts — runLayer2 compareCountryOfOrigin should now use the extracted value instead of the placeholder needs_review note.\n- EDIT: src/core/providers/stub.ts — add countryOfOrigin to fixtures (undefined or blank for domestic; could add an import fixture later).\n- EDIT: src/worker/index.ts — import both providers; choose based on process.env['GEMINI_API_KEY'] presence; log which is in use at startup.\n- NEW or EDIT: .env.example with GEMINI_API_KEY=.\n- EDIT: README.md — one-line note that real extraction requires GEMINI_API_KEY; without it, the worker falls back to the stub.\n\nSDK: @google/genai is the current package name as of 2026 (verify against current docs — knowledge cutoff may be stale; older docs reference @google/generative-ai). Model id: latest Gemini 3.x Pro vision tier (verify; expect gemini-3-pro or similar). Auth via API key in env.\n\nIMAGE INPUT: read file as bytes from imagePath; pass via inline_data (base64) for files <20MB. Composites in fixtures/labels/ are <600KB so inline is fine. No need to use the Files API.\n\nSTRUCTURED OUTPUT: use responseMimeType: 'application/json' + responseSchema. Schema fields:\n  brandName, classType, alcoholContent, netContents, producerName, producerAddress, countryOfOrigin (optional), governmentWarning.\nEach: { value: string | null, confidence: 'low'|'med'|'hi', confidenceRaw: number | null }.\n\nPROMPT (place in a const at top of gemini.ts):\n'This image shows the front and/or back panels of an alcohol bottle label, possibly as a side-by-side or stacked composite of two panels. Extract each listed field as it appears on the label. Return null for any field not visible in the image. Do not hallucinate or infer missing values from training data — if the back panel is not visible, the government warning field must be null. For each field, return extraction_confidence: low (blurry, partial, glare-obscured, hard to read), med (readable but stylized or low-contrast), or hi (clean print, unambiguous).'\n\nNULL HANDLING: when Gemini returns null for a field's value, transform to { value: '', confidence: 'low', confidenceRaw: null }. runLayer1's checkRequiredField already treats empty value as fail (mandatory field missing). countryOfOrigin null → leave as undefined (it's optional).\n\nCONFIDENCE_RAW: if the SDK returns logprobs or a numeric self-rating, map to 0-1. If only the bucketed string is returned, derive confidenceRaw as 0.3 (low) / 0.7 (med) / 0.95 (hi) for now — better than null for downstream tuning, and the bucketed value is what the UI uses anyway.\n\nOUT OF SCOPE FOR THIS BEAD: prompt iteration / model choice tuning beyond making it work; per-field side-by-side detail UI (separate P1 bead); production deploy (separate bead T8).","acceptance_criteria":"1. npm install succeeds with the chosen Gemini SDK package added.\n2. src/core/providers/gemini.ts exists implementing LabelProvider.analyzeLabel(imagePath).\n3. With GEMINI_API_KEY set in env, dropping fixtures/labels/cointreau.jpg + fixtures/applications/cointreau.json into data/incoming/ produces Layer 1 + Layer 2 findings whose values reflect what is actually printed on the cointreau bottle (not the good-merlot defaults).\n4. Same test passes for agave and rumble fixtures.\n5. Without GEMINI_API_KEY set, src/worker/index.ts falls back to StubLabelProvider and logs which provider is in use at startup.\n6. ExtractedFields includes countryOfOrigin; Layer 2 compares it normally when present in both extraction and application; reports needs_application_data when missing from application.\n7. Empty/null values from Gemini map to { value: '', confidence: 'low', confidenceRaw: null }, and Layer 1's checkRequiredField marks them as fail with the standard mandatory-field-missing message.\n8. .env.example contains GEMINI_API_KEY= (no value).\n9. README.md briefly notes the env var requirement and the stub fallback.\n10. npm run typecheck && npm run lint && npm run test all pass.","status":"closed","priority":0,"issue_type":"task","assignee":"gjw","created_at":"2026-05-01T23:51:29.949838Z","created_by":"gjw","updated_at":"2026-05-02T03:13:27.655283Z","closed_at":"2026-05-02T03:13:27.655104Z","close_reason":"Wired GeminiLabelProvider with @google/genai (gemini-3.1-pro-preview); structured-JSON output, null→empty for required fields; countryOfOrigin added as optional ExtractedField with full Layer 2 comparison; worker selects provider by GEMINI_API_KEY presence; 3 fixture application JSONs added; verified end-to-end against 3 real bottle photos.","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"]}
{"id":"cc-9cx","title":"Layer 1 ABV regex too narrow; rejects formats Gemini commonly returns","description":"Observed during cc-7w9 live testing: alcoholContent Layer 1 fails for cointreau (40%), agave (no ABV on photo, expected fail), and rumble (47%) — Cointreau and Rumble both have clearly readable ABVs on the label.\n\nThe two regexes in src/core/verification.ts (ABV_FORMAT and ABV_SHORTHAND) require the literal token 'alcohol' or 'alc' to appear adjacent to the percentage. Gemini-returned strings like '40% Alc/Vol', 'ALC. 47% BY VOL.', or '40% ABV' likely fall outside these patterns.\n\nNeed to broaden parseAbv to accept:\n- 'X% ABV' (no 'vol' / 'volume' suffix)\n- 'ALC. X% BY VOL.' (period on 'alc.', period on 'vol.')\n- 'X% Alc/Vol' (no leading 'alc')\n- 'X%ALC/VOL' (no spaces)\n\nUntil this is fixed, every real Gemini extraction reports Layer 1 alcohol-format failure regardless of what's actually on the label, which masks both real failures and successes.\n\nSuggested approach: build a small fixture of Gemini-observed ABV string variants from the live test runs, then iterate the regex against it. Consider extracting parseAbv into its own module so we can keep the test file focused.","notes":"Bumped to P0: every real Gemini extraction currently fails Layer 1 ABV format, making the rule engine look broken on real labels. Demo-blocking. 30-minute regex fix.","status":"closed","priority":0,"issue_type":"bug","assignee":"gjw","created_at":"2026-05-02T02:57:43.821013Z","created_by":"gjw","updated_at":"2026-05-02T18:44:13.798822Z","closed_at":"2026-05-02T18:44:13.798619Z","close_reason":"Broadened ABV_SHORTHAND to accept alc. period variants and added optional proof tail to both regexes; fixture-driven tests from real Gemini output (6 labels) prevent future regex regressions.","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"]}
{"id":"cc-ckg","title":"Linode deploy: nginx vhost, PM2 ecosystem, env config, dry-run on linode43393","description":"Live URL https://colacop.foramerica.dev needs to actually serve the prototype. Existing infrastructure on linode43393 (Linode 16GB Dallas): nginx + PM2 already installed, Postgres via docker compose. Wildcard *.foramerica.dev Let's Encrypt cert already issued. Build: ssh + git pull + npm ci + migrate + build + pm2 reload, per ARCHITECTURE.md > Deployment Shape > Live deployment target.","design":"ARTIFACTS TO PRODUCE:\n- ecosystem.config.cjs at repo root. Two PM2 apps: 'colacop-server' (npm run start:server) and 'colacop-worker' (npm run start:worker). Each with cwd, NODE_ENV=production, env_file pointing at /opt/colacop/.env, restart policy.\n- nginx vhost at /etc/nginx/sites-available/colacop.foramerica.dev — reverse proxies / to localhost:<port>. Static frontend served either by Express or by nginx directly (TBD by Trench during execution; Express is simpler to start with).\n- .env.production template at /opt/colacop/.env (NOT committed); committed counterpart .env.example at repo root with all required keys (GEMINI_API_KEY, DATABASE_URL, PORT, DATA_DIR, INCOMING_DIR, etc., per ARCHITECTURE.md > Deployment Shape).\n- README in repo root and ARCHITECTURE.md > Deployment Shape already document the manual deploy command sequence; no doc change unless something diverges.\n\nDEPLOY DRY-RUN (the work):\n- ssh to root@45.33.122.158 (linode43393).\n- mkdir /opt/colacop; clone the repo there.\n- nodenv install (.node-version pins the version).\n- docker compose up -d db.\n- npm ci.\n- npm run migrate.\n- npm run build (produces the Vite bundle for src/web).\n- pm2 start ecosystem.config.cjs.\n- nginx vhost + 'nginx -t' + 'systemctl reload nginx'.\n- Verify https://colacop.foramerica.dev resolves to the running prototype with the existing wildcard cert.\n\nDEPS: cc-7w9 (Gemini wired) — production needs the real provider; without it the deployed app would only show stub results.\n\nOUT OF SCOPE: CI/CD, automated deploys, blue-green or zero-downtime swaps, monitoring/alerting.","acceptance_criteria":"1. ecosystem.config.cjs committed at repo root; pm2 start succeeds locally if PM2 is installed.\n2. .env.example committed; .env on the box is created manually with the real values; .env is gitignored.\n3. nginx vhost config is committed (e.g. as deploy/nginx/colacop.foramerica.dev.conf) so it is reproducible; the live config on the box is symlinked/copied from there.\n4. https://colacop.foramerica.dev returns the running prototype (HTTP 200, the React SPA loads).\n5. Dropping a fixture pair into /opt/colacop/data/incoming/ on the live box produces a job visible at https://colacop.foramerica.dev within seconds.\n6. The deploy command sequence in README.md actually works as written when followed by Chair from a clean shell.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-05-01T23:52:40.146699Z","created_by":"gjw","updated_at":"2026-05-03T19:16:26.171754Z","closed_at":"2026-05-03T19:16:26.171595Z","close_reason":"Deployed and live at https://colacop.foramerica.dev as of 2026-05-03.\n\nWhat landed (3663ecf):\n- ecosystem.config.cjs (PM2: colacop-server + colacop-worker via tsx)\n- deploy/nginx/colacop.foramerica.dev.conf\n- docker-compose.yml: parameterized port via ${POSTGRES_PORT:-5432}\n- package.json: build alias\n- README: corrected deploy host/path\n\nWhat landed before (70f439e): .env loading via process.loadEnvFile(),\n.gitignore for entire .claude/, CONTEXT.md S-001..S-007.\n\nBox state: ChatBridge fully torn down (PM2 deleted, container removed,\nnginx vhosts unsymlinked). PM2 colacop-server + colacop-worker online\nunder pm2-root.service so they survive reboot. Cert valid through\n2026-08-01, auto-renewing. Reboot persistence not yet exercised.\n\nAcceptance criteria — context-adjusted from the description:\n1. ecosystem.config.cjs committed, pm2 starts cleanly. ✓\n2. .env.example committed, prod .env created via ssh stdin (chmod 600,\n   not in git). ✓\n3. nginx vhost committed at deploy/nginx/colacop.foramerica.dev.conf,\n   live config symlinked. (Note: certbot edited the live file to add\n   the SSL block — committed source remains the HTTP-only initial form.\n   Re-cp would need to be followed by certbot --nginx.) ✓\n4. https://colacop.foramerica.dev returns the SPA, 200. ✓\n5. Fixture pair into data/incoming/ produces a job within ~2s, processed\n   in ~6s with real Gemini extractions (brand=ESPOLÒN etc.). ✓\n6. README deploy command sequence works (path corrected to ~/colacop).\n\nDescription deviations from reality (description was wrong, reality matches\nChair's intent):\n- /opt/colacop → /root/colacop (mirror chatbridge convention)\n- linode43393 / 45.33.122.158 → agentforge / 45.33.3.111\n- \"wildcard *.foramerica.dev cert already issued\" → false; certbot\n  HTTP-01 issued a fresh cert for colacop.foramerica.dev\n\nPath/file-lifecycle research surfaced during this work (data/processing,\ndata/processed, data/failed scaffolded but unused; application JSON\nread live from disk not stored in DB; image URL coupled to basename +\nincomingDir) is captured as a comment on cc-1c5.","source_repo":".","compaction_level":0,"original_size":0,"labels":["deployment","mvp"],"dependencies":[{"issue_id":"cc-ckg","depends_on_id":"cc-7w9","type":"blocks","created_at":"2026-05-01T23:53:53.423591Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-egk","title":"Worker should refuse to start if another worker is already bound to the same DATABASE_URL/INCOMING_DIR","description":"Observed during cc-7w9 live testing: stale 'tsx watch src/worker/index.ts' processes from earlier dev sessions silently raced with my fresh worker. Both watched the same data/incoming and both inserted into the same Postgres. On a single fixture drop, chokidar fired add events to all running workers; the stub-backed stale worker won the race for Layer 1 (16ms — clearly stub) and the new Gemini-backed worker overwrote Layer 2 with a real extraction. The combined record looked corrupt (Layer 1 said '13.5% ABV pass' against the actual Cointreau label) and was confusing to debug.\n\nThere's no single-writer guarantee in the worker right now. ps aux is the only way to notice.\n\nSuggested approach: take an advisory lock on a stable key (e.g., pg_advisory_lock(hashtext(INCOMING_DIR))) at worker startup. If the lock can't be acquired, log and exit non-zero rather than running silently. Optionally: also lock per-job-stem during processStem to harden against accidental double-starts in future deploy mishaps.\n\nOut of scope: orchestrating multiple workers across machines (PM2 fork mode, etc.) — that needs a separate design pass.","status":"open","priority":2,"issue_type":"chore","created_at":"2026-05-02T03:27:35.672506Z","created_by":"gjw","updated_at":"2026-05-02T03:27:35.672506Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"cc-ff6","title":"Pick SQL access library: Knex vs Kysely","description":"ARCHITECTURE.md currently picks Knex; Chair is leaning toward Kysely. Tower writes a short comparison covering: type-safety with TS-strict (Kysely's strength), migration ergonomics (Knex's strength, mature CLI), reviewer familiarity (Knex more conventional, Kysely growing), ecosystem maturity, exit cost if we change later. Output: a 1-paragraph decision recorded in ARCHITECTURE.md and a rewrite of the existing Knex mentions in 'Chosen Stack', 'Why This Stack', and 'Database Responsibilities' sections. Blocks cc-scz so scaffolding doesn't bake in the wrong choice.","notes":"Decision: Kysely. Rationale: schema-first type inference aligns with the project's strict TS rules (no any, no casts, noUncheckedIndexedAccess); the shared Database interface doubles as documentation between API and worker; column typos become compile errors. Migration tooling gap (no Knex-style CLI) is small at our scale — a tsx-driven Migrator script is sufficient for ~4-6 expected migrations. Reviewer surprise minimal in 2026. ARCHITECTURE.md updated; cc-scz design notes updated to install kysely + pg + tsx.","status":"closed","priority":0,"issue_type":"decision","created_at":"2026-04-29T18:42:08.182542Z","created_by":"gjw","updated_at":"2026-04-29T20:32:44.241468Z","closed_at":"2026-04-29T20:32:44.241003Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["decision","scaffolding"]}
{"id":"cc-fk3","title":"Author Cockburn-style use cases","description":"Critical path: write UC-001 (Verify an alcohol label application) in design/use-cases.yaml so architecture and the prototype scope can be checked against an actor-and-goal-level description. cc-xog has shipped a working stub of the system without a use case ever being written; re-grounding the design via UC-001 is the first action in the May 1 replan. Cockburn-style: ONE main use case with rich extensions, NOT a scattershot of UC-001/UC-002/UC-003. Source material is the existing facts and requirements; do NOT look at src/ or the running app while drafting (that anchors the use case on the implementation rather than on the agent's goal).","design":"FRESH TOWER: read CLAUDE.md, prompts/tower.md, CONTEXT.md, ARCHITECTURE.md, then discovery/sources/S-001..S-005, discovery/facts.yaml, design/requirements.yaml, docs/handoff.md. Do NOT open src/, the running UI, or browse the deployed URL — those anchor the use case on the implementation instead of the agent's goal.\n\nOUTPUT: create design/use-cases.yaml (file does not yet exist). Schema:\n\nuse_cases:\n  - id: UC-001\n    title: 'Verify an alcohol label application'\n    primary_actor: 'TTB compliance agent'\n    scope: 'colacop prototype'\n    level: 'user goal'    # Cockburn level\n    preconditions:\n      - '...'\n    success_guarantee:\n      - '...'\n    main_success_scenario:\n      - step: 1\n        actor_action: '...'\n        system_response: '...'\n    extensions:\n      - id: 'UC-001.3a'   # extends step 3 of main scenario\n        condition: '...'\n        steps:\n          - '...'\n    supports_requirements:\n      - REQ-001\n    references_facts:\n      - F-004\n    out_of_scope:\n      - '...'\n    open_questions:\n      - '...'\n\nEXTENSIONS TO COVER (per docs/handoff.md, expand or merge as needed): submitted file already processed; submitted file still queued or processing; batch of files arrives; extracted field clearly matches application data; extracted field clearly mismatches application data; brand differs only by case/punctuation and needs tolerant matching; government warning text/capitalization fails strict matching; label image is unreadable or low confidence; model provider is unavailable or blocked; application data arrives without label; label arrives without application data.\n\nLEVEL DISCIPLINE: stay at user-goal level. Refer to 'the system' not 'the watcher', 'the queue' not 'data/incoming/', 'the verification result' not 'Layer 1 / Layer 2 / lifecycle / verdict'. Architecture-shaped detail (lifecycle states, verdict enums, watcher mechanics, Kysely schema) belongs in ARCHITECTURE.md and stays there. The use case is an agent's narrative, not a sequence diagram.\n\nCITATIONS: every extension must cite at least one supporting fact (F-XXX) and at least one supported requirement (REQ-XXX). If you find an extension that lacks fact backing, flag it in open_questions — do not invent fact provenance.\n\nFOLLOW-UP: a separate bead covers the WILL/WON'T/MAYBE scope pass and the structured audit. Do not try to do those here — UC-001 first, full-scope (canonical product), then the next session decides what's in for May 1 submission.","acceptance_criteria":"1. design/use-cases.yaml exists and parses as YAML. 2. UC-001 is present with all Cockburn fields (id, title, primary_actor, scope, level, preconditions, success_guarantee, main_success_scenario, extensions, supports_requirements, references_facts, out_of_scope, open_questions). 3. Main success scenario is 6-12 numbered steps with actor_action + system_response per step. 4. Extensions cover the 11 listed cases (or merge them with explicit rationale). 5. Each extension cites at least one F-XXX fact and one REQ-XXX requirement. 6. No reference to lifecycle enum values, verdict enum values, watcher specifics, Kysely, React, Express, or any other implementation detail. 7. Ruby validation passes: ruby -e 'require \"yaml\"; YAML.load_file(\"design/use-cases.yaml\"); puts \"YAML OK\"'. 8. Cross-ref check: all F-XXX in references_facts exist in discovery/facts.yaml; all REQ-XXX in supports_requirements exist in design/requirements.yaml.","notes":"UC-001 authored and committed in design/use-cases.yaml (66b64c4, aff2d54). Cockburn-style with main success scenario + 11 extensions covering batch arrival, application-before-label, label-before-application, in-flight, model-unavailable, missing mandatory field, warning violation, low-confidence read, clear match, clear mismatch, brand drift, label-only, and per-field application gap. Each extension cites supporting facts (F-XXX) and supports requirements (REQ-XXX). Out_of_scope and open_questions sections preserved.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-04-29T18:37:21.180695Z","created_by":"gjw","updated_at":"2026-05-01T23:55:25.439270Z","closed_at":"2026-05-01T23:55:25.438887Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["discovery"],"dependencies":[{"issue_id":"cc-fk3","depends_on_id":"cc-5ea","type":"blocks","created_at":"2026-04-29T18:37:28.381059Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-fsa","title":"Gemini strips parenthetical proof tail from ABV extractions","description":"Observed during cc-9cx live capture: Cointreau label shows '40% ALC./VOL. (80 PROOF)' but Gemini returned '40% alc./vol.'; Rumpleminze label shows '50% ALC/VOL (100 PROOF)' but Gemini returned '50% ALC/VOL'. The prompt instructs 'extract each listed field as it appears on the label' but the model is canonicalizing.\n\nNot currently blocking — parseAbv (post-cc-9cx) handles both with-proof and without-proof inputs equivalently. But it means we can't faithfully report 'label says X' if X includes proof.\n\nLikely fix: prompt-tuning. Try 'Return the alcoholContent field VERBATIM as printed, including any parenthetical proof statement.' Verify against fixtures/labels/cointreau.jpg and fixtures/labels/rumple.jpg by re-running scripts/capture-extractions.ts.\n\nCaptured extractions in fixtures/raw/ from 2026-05-02 are the reference baseline.","status":"open","priority":2,"issue_type":"bug","created_at":"2026-05-02T18:44:09.801512Z","created_by":"gjw","updated_at":"2026-05-02T18:44:13.694832Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["post-mvp"]}
{"id":"cc-g87","title":"Surface stub-provider fallback in UI so silent merlot extraction is visible","description":"Demo-blocker root cause for cc-vho session: when GEMINI_API_KEY is unset/empty, src/worker/index.ts silently falls back to StubLabelProvider, which returns FIXTURES['good-merlot'] (Stone's Throw Merlot) for any unknown stem. Result: every real bottle (cointreau, rumble, fireball, rumple, shinok, agave) extracts as Stone's Throw Merlot; Layer 2 fails comparing 'Merlot' to the real application JSON, looking like Layer 2 is checking against a merlot fixture.\n\nThe fallback itself is intentional (CI/tests want it), but the only signal that stub is in use is a startup log line: '[worker] provider: StubLabelProvider (GEMINI_API_KEY not set)'. Easy to miss; reviewer agent has no in-UI cue.\n\nSuggested fix:\n- Persist provider name on the job row when processed (new column, e.g. extraction_provider: 'gemini' | 'stub').\n- JobDetailView shows a tag like 'extracted by: stub fixtures' next to the lifecycle badge when provider != 'gemini'.\n- Optional: refuse to start the worker when expected provider (real Gemini) is misconfigured if a STRICT_PROVIDER=true env flag is set, so production deploys can't silently regress to stub.\n\nTrigger for filing: a single-character bug in user's local .envrc ('GEMINI_API_KEY=…' instead of 'export GEMINI_API_KEY=…') made direnv set the var locally but not export it. Worker silently fell back to stub. Reviewer experience: 'why is everything Merlot?'","status":"open","priority":1,"issue_type":"bug","created_at":"2026-05-02T22:45:44.560914Z","created_by":"gjw","updated_at":"2026-05-02T22:46:57.182260Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["bug"]}
{"id":"cc-hfv","title":"Reconcile docs/handoff.md drift vs ARCHITECTURE.md","description":"handoff.md still says TS-vs-Clojure and SQLite-vs-Postgres are open; ARCHITECTURE.md has locked TypeScript and Postgres-in-Docker. Update or trim handoff to reflect locked decisions so future readers don't see contradictions.","status":"open","priority":2,"issue_type":"task","created_at":"2026-04-29T18:37:21.232398Z","created_by":"gjw","updated_at":"2026-04-29T18:37:21.232398Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["docs"]}
{"id":"cc-htd","title":"Build sample-label fixtures and application-data fixtures","description":"Build a realistic fixture set covering the main verification paths. Source mix: real bottles from Chair's cabinet (preferred for realism per assignment F-039/F-040/F-041 — Jenny Park's interview specifically calls out 'labels photographed at weird angles, lighting is bad, glare on the bottle') and/or AI-generated labels (preferred for absolute-baseline-correct happy path). Each fixture is one image (.jpg/.png/.webp) paired with one application-data JSON sharing a filename stem. Necks are out of scope for this prototype — document in README; in the real TTB COLA system labels include front/back/neck panels but we treat one composite image as one submission. Real bottles are typically photographed as a front+back composite (stitched in Preview) so the government warning panel is included; some fixtures may intentionally omit the back panel to exercise the missing-warning path.","design":"FIXTURE COMPOSITION (target ~6-8 fixtures):\n- 2-3 clean front+back composites (real bottles or AI-generated) with well-formed application JSON that matches the label exactly. Exercises the happy-path 'all WILL paths succeed' demo.\n- 1 fixture where brand name on the label differs from the application by case/punctuation only (e.g. 'STONE'S THROW' on label, 'Stone's Throw' in JSON) per F-030. Exercises Layer 2 brand drift / fuzzy match.\n- 1 fixture where the application's alcohol_content disagrees with the label's printed value beyond the F-052 wine tolerance. Exercises Layer 2 mismatch with rule-based explanation.\n- 1 fixture intentionally missing the back panel (front-only photo). Exercises Layer 1 missing-government-warning path; demonstrates honest reporting rather than hallucination.\n- 1 fixture photographed badly on purpose (glare, angle, low contrast, or stylized/foil label). Exercises REQ-009 / F-056 low extraction_confidence path; the system should flag low confidence rather than guess.\n- 1 fixture where the label's government warning has a real violation (lowercase 'Government Warning' instead of all-caps, or near-text-but-not-exact wording, per F-047/F-048 and Jenny's interview). Exercises Layer 1 warning strict-match failure.\n\nSTORAGE: fixtures/ at repo root. Subdirs: fixtures/labels/ (the .jpg/.png pairs), fixtures/applications/ (the .json pairs sharing stems), fixtures/README.md (one paragraph per fixture: stem, source, what it tests, expected verdict pattern).\n\nNO PII / COPYRIGHT CONCERNS: real bottles are Chair's own; this is take-home demo use, not commercial distribution. Brand names are real; that is fine.\n\nGEMINI PROMPTING (informs cc-htd boundary, executed in the Gemini-wiring bead later): the prompt to analyzeLabel should explicitly state 'this image shows the front and/or back panels of an alcohol bottle label, possibly as a side-by-side or stacked composite. Extract the listed fields. Return null for any field not visible. Do not hallucinate missing fields.' Use Gemini's native structured-output / JSON-schema mode rather than freeform-text-plus-parse. Per-field extraction_confidence (low/med/hi) is part of the schema. The extraction prompt is NOT cc-htd's responsibility but the fixtures should be designed to produce predictable behavior under that prompt.","acceptance_criteria":"1. fixtures/ directory exists with labels/ and applications/ subdirs and a README.md. 2. At least 6 paired fixtures exist; each label image has a stem-matching JSON file. 3. Each ApplicationData JSON validates against the schema in src/core/schemas.ts (or wherever Zod has it after xog). 4. fixtures/README.md describes each fixture's intent in 1-2 lines: what scenario, what verdict pattern is expected. 5. At least one fixture for each of: clean match, brand drift, ABV mismatch, missing back panel, low-extraction-confidence, government warning violation. 6. Real-bottle composites (if used) are stitched front+back JPGs, not separate files. 7. README.md (top-level repo) is updated with a one-line assumption: 'Each submission is a single image. Real TTB filings include separate front/back/neck panels; the prototype treats one image — typically a front+back composite — as one submission. Necks are not in scope.'","status":"open","priority":1,"issue_type":"task","created_at":"2026-04-29T18:37:21.463065Z","created_by":"gjw","updated_at":"2026-05-01T19:20:25.023878Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["fixtures"]}
{"id":"cc-idm","title":"Reset-to-demo-state secondary affordance: drop all jobs/decisions, reseed fixtures, watcher reprocesses","description":"After cc-1c5 lands the upload-driven workflow as the primary reviewer affordance, there is still real value in a small 'Reset to demo state' button for cleanup between reviewers and recovery from accidental uploads. NOT a primary button — a discreet link in a footer or admin strip, behind a confirmation modal that lists exactly what gets dropped. Build only with Sunday afternoon slack; the demo works fine without it.","design":"BACKEND:\n- POST /api/admin/reset (or /api/demo/reload-fixtures — pick a name that matches the button label).\n- In a transaction: DELETE FROM decisions; DELETE FROM layer1_results; DELETE FROM layer2_results; DELETE FROM jobs.\n- Wipe data/incoming/, data/processing/, data/processed/, data/failed/ contents (keep the dirs and .gitkeep files).\n- Run the seed routine from cc-1c5 to copy fixtures back into data/incoming/.\n- Watcher's chokidar fires; worker re-extracts via Gemini; results repopulate.\n\nUI:\n- Small 'Reset demo data' link in a footer or top-right admin strip. Not a big button. Discreet.\n- Confirmation modal: 'Reset to demo state? This drops all current jobs, decisions, and findings; copies the 6 demo bottles back into the queue; and re-runs verification. Takes about 10 seconds.' Two-step click.\n- Optional: gate behind an env flag (DEMO_MODE=true) so local development can disable it. For the prototype's live URL, always enabled.\n\nRISKS ACCEPTED:\n- Public-URL abuse: cost ~/bin/zsh.03/reset; 200 resets = ~/bin/zsh; within budget.\n- Mid-review interruption: confirmation modal mitigates; if a reviewer spams, that's their loss.\n- Worker race during reset: drop+reseed is one transaction; chokidar event is naturally next-tick; no in-flight job collides with the reset.\n\nDEPS: cc-1c5 (shares the seed routine and download infrastructure).","acceptance_criteria":"1. POST /api/admin/reset (or chosen name) drops all jobs/decisions/results, wipes data/incoming/processing/processed/failed contents, copies fixtures back to data/incoming/.\n2. The watcher reprocesses the seeded fixtures; within ~10 seconds the queue is back to the demo state.\n3. UI exposes the action as a discreet link, NOT a primary button; clicking shows a confirmation modal listing what will happen; only on confirm is the action triggered.\n4. typecheck/lint/test pass.","status":"open","priority":2,"issue_type":"task","created_at":"2026-05-03T02:58:35.560649Z","created_by":"gjw","updated_at":"2026-05-03T02:58:57.502178Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["deployment"],"dependencies":[{"issue_id":"cc-idm","depends_on_id":"cc-1c5","type":"blocks","created_at":"2026-05-03T02:58:57.442089Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-jhk","title":"Scope + audit pass: WILL/WON'T/MAYBE for May submission, gap beads from xog audit","description":"After UC-001 lands in design/use-cases.yaml, do the scope pass and the structured audit in one Tower session. Output: notes/scope-may-2026.md with WILL/WON'T/MAYBE annotations on each UC-001 step + extension; notes/audit-2026-05-01.md with the per-criterion pass/partial/fail/unverified table for cc-xog acceptance criteria 1-8 plus a use-case-alignment column for each WILL step; and a batch of new beads for each WILL gap (P0) and SHOULD gap (P1).","design":"INPUTS: design/use-cases.yaml (UC-001 from cc-fk3), discovery/facts.yaml, design/requirements.yaml, ARCHITECTURE.md, the running prototype (now allowed — this session's job IS to compare spec vs implementation), notes/sprint-order.md WILL/WON'T/MAYBE strawman.\n\nOUTPUTS:\n- notes/scope-may-2026.md: each UC-001 main step + extension marked WILL / WON'T / MAYBE for the Monday submission, with a one-line rationale per non-trivial call.\n- notes/audit-2026-05-01.md: a table walking the 8 cc-xog acceptance criteria with columns 'pass / partial / fail / unverified', a 'notes' column, and a separate table walking each WILL step from UC-001 with columns 'covered by built code? y/n/partial', 'evidence', 'gap'.\n- new beads for each WILL gap (P0) and each SHOULD gap (P1). MAYBE gaps that didn't make scope: P3 with rationale.\n\nPROCESS:\n1. Read UC-001 first. Ground in the actor's goal.\n2. Mark scope on each step/extension. Use the strawman in notes/sprint-order.md as a starting point but treat it as inputs to be reviewed, not committed.\n3. Run the functional audit — drop fixtures into data/incoming/, walk the UI, exercise each cc-xog acceptance criterion. Record outcomes in the audit doc.\n4. Run the use-case audit — for each WILL step, can the agent actually accomplish it through the built code? Record gaps.\n5. File gap beads. Use cc- prefix; one bead per discrete fixable thing. No omnibus 'fix everything' beads.\n\nDO NOT in this session: implement fixes, write fixtures, wire up Gemini. Gap beads go on the queue; execution happens in subsequent Trench sessions.","acceptance_criteria":"1. notes/scope-may-2026.md exists with WILL/WON'T/MAYBE marked on every UC-001 step and extension, each with a one-line rationale. 2. notes/audit-2026-05-01.md exists with two tables: 8-row functional table covering cc-xog acceptance criteria, N-row use-case table covering UC-001 WILL steps. 3. Every WILL gap from the audit has a corresponding new P0 bead with description + design + acceptance fields filled in. 4. Every SHOULD gap has a corresponding new P1 bead. 5. Each new bead links back to the audit document and the relevant UC-001 step/extension. 6. cc-xog status: closed if all 8 acceptance criteria are at least partial-pass with the partials beaded; left in_progress if any criterion is fail-with-no-bead-filed.","notes":"Audit deliverable committed in notes/audit-2026-05-01.md (8b0b4c7). Functional axis: 7/8 cc-xog acceptance criteria pass; #7 typecheck/lint/test unverified by Tower (Chair to confirm). Use-case axis: five real gaps surface against UC-001 — decision step 6 missing entirely (cc-syy), label image not rendered (cc-vho), Gemini not wired (cc-7w9), low-confidence collapsed into verdict (cc-ye9), per-field needs_application_data broken (cc-srj). Plus deploy (cc-ckg), empty-queue (cc-1c5), and Layer 2 side-by-side (cc-55c). Scope decisions are recorded in notes/sprint-order.md as the strawman WILL/WON'T/MAYBE; not duplicated into a separate scope-may-2026.md because the audit's bead recommendations and sprint-order's strawman together cover the same ground.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-05-01T18:17:08.643043Z","created_by":"gjw","updated_at":"2026-05-01T23:55:15.135581Z","closed_at":"2026-05-01T23:55:15.135157Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"cc-jhk","depends_on_id":"cc-fk3","type":"blocks","created_at":"2026-05-01T18:17:34.496883Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-lm6","title":"Worker: retry with backoff on Gemini 429 RESOURCE_EXHAUSTED","description":"Gemini API enforces 25 RPM on gemini-3.1-pro regardless of spend. Bursting past this (e.g., processing all watched-directory fixtures simultaneously, or after a worker restart that re-queues several jobs) currently fails the job permanently with failure_reason set to the raw 429 JSON — the reviewer would see 'lifecycle: failed' on perfectly-valid labels. First observed 2026-05-02 while verifying cc-o1k (re-queue of all 6 fixtures hit 429 on shinok).\n\nNeed exponential backoff that honors the retryDelay hint in the 429 response.\n\nImplementation sketch:\n- In src/core/providers/gemini.ts analyzeLabel, catch errors where the response code is 429 / status is RESOURCE_EXHAUSTED.\n- Parse details[].retryDelay (e.g. '9s') — fall back to exponential backoff (1s, 2s, 4s, 8s) if absent.\n- Cap at ~3 retries (~30s total wall time) to avoid masking real outages.\n- Bubble up after exhaustion so the job still ends in failed rather than hanging.\n- Test with a mock provider that returns 429 N times then succeeds.\n\nAcceptance:\n1. Worker survives a single 429 burst without marking jobs failed.\n2. After 3 consecutive 429s, the job fails with a clear reason (not raw JSON).\n3. Unit test covers retry-then-succeed and retry-exhaustion paths.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-05-03T03:36:10.776296Z","created_by":"gjw","updated_at":"2026-05-03T03:36:18.752183Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"]}
{"id":"cc-o1k","title":"UI: surface Gemini's extracted value on every Layer 1/Layer 2 finding","description":"Currently a finding shows verdict (pass/fail/needs_review), confidence badge, and a prose message. The agent cannot see what Gemini actually extracted, which makes any 'fail' verdict opaque — they must trust or re-run.\n\nConcrete user-visible gap: fireball's alcoholContent is reporting 'fail — does not conform to 27 CFR 4.36 format' but neither the agent nor Trench can see what Gemini returned that failed to parse. The offline capture in fixtures/raw/fireball.extraction.json has 'ALC. 16.5% BY VOL.' (which the post-cc-9cx regex accepts cleanly), so the live extraction returned something different. We have no way to know what.\n\nWHAT NEEDS TO HAPPEN:\n\nBackend (schema + persistence):\n- layer1_results: add extracted_value text column (nullable for fields where extraction failed entirely vs returned something the parser rejected — distinguish null from empty string).\n- layer2_results: add extracted_value AND application_value text columns (Layer 2 is a comparison; the agent needs to see both sides).\n- Migration in scripts/migrate.ts; persistLayer1/persistLayer2 in src/worker/repo.ts populate the new columns; jobs route returns them in the detail response.\n\nUI:\n- JobDetailView field-row gets a third or fourth subline showing 'extracted: <value>' for Layer 1 and 'extracted: <value> | application: <value>' for Layer 2.\n- Distinguish missing-from-label (null) from extracted-but-malformed (string) visually.\n- Long extracted strings (e.g. governmentWarning's full text) need truncation+expand or a tooltip/modal — they will blow out the field-row layout otherwise.\n\nOUT OF SCOPE for this bead: per-finding reviewer actions (filed separately as a scope question — see follow-up bead). Restructuring messages to drop value embedding ('Label X does not match application Y' currently bakes the values into prose; can stay until a separate cleanup pass).\n\nAcceptance criteria:\n1. Migration adds extracted_value to layer1_results, extracted_value + application_value to layer2_results; npm run migrate succeeds.\n2. After processing, the job detail JSON for any field where Gemini returned a non-null value includes the extracted text.\n3. JobDetailView renders the extracted value (and, for L2, the application value) under each finding's message.\n4. Fireball's alcoholContent finding shows the live extraction string in the UI, making the false-fail diagnosable.\n5. typecheck/lint/test pass; existing tests still green; new test asserts extracted_value persisted from runLayer1/runLayer2 outputs.\n\nNOTE for groomer: this bead covers the persistence + display path. Per-finding decisions and the fireball root-cause investigation are separate beads.","notes":"Promoted to P0 by Tower 2026-05-02: gates demo credibility (agent seeing 'fail' with no extracted value can't tell whether the label is bad or the system is broken) and unblocks cc-7v3 diagnosis (fireball false-fail is much easier to root-cause once we can see what Gemini actually returned). Subsumes most of cc-55c, which is demoted to P2 polish-on-top.","status":"closed","priority":0,"issue_type":"task","assignee":"gjw","created_at":"2026-05-03T02:46:04.701336Z","created_by":"gjw","updated_at":"2026-05-03T13:09:47.805865Z","closed_at":"2026-05-03T13:09:47.805715Z","close_reason":"UI now surfaces Gemini's extracted_value on every L1/L2 finding, plus application_value on L2; backend persists via new migration; verified end-to-end with all 6 fixtures.","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"]}
{"id":"cc-q25","title":"Scope: per-finding reviewer actions (approve/deny single fields) vs label-level decision only","description":"OPEN QUESTION — needs Tower+Chair scope review before any implementation.\n\nCurrent model (UC-001 + cc-syy): the agent reviews all findings and makes one binary-ish decision at the label level (approve / reject / send_back). Individual fields with verdict='needs_review' have no per-finding affordance — there's no UI button next to a 'needs_review' brand-name drift to say 'I accept this drift'.\n\nThe question Chair raised during cc-vho review: should the prototype let a reviewer act on individual findings — e.g. 'I approve this brandName drift' or 'I reject this ABV mismatch as a hard fail' — separately from the overall label decision?\n\nTwo reasonable interpretations:\n\nA. LABEL-LEVEL ONLY (current cc-syy model). Reviewer reads all findings + image + application data, then makes one decision for the whole label. Findings are evidence; decisions are about the submission. Matches the TTB COLA flow as described in the assignment (one label = one COLA action). Simpler.\n\nB. PER-FINDING + LABEL-LEVEL. Reviewer can mark individual findings as 'override-pass' / 'override-fail' / 'noted'; the system then derives a recommended label-level decision from per-finding states; reviewer can still override at label level. Closer to how a COLA inspector might actually work an annotation-style review.\n\nConsiderations:\n- Take-home time budget: B is meaningfully more state and UI than A.\n- The agents-as-helpers framing (Jenny Park interview, F-039 et al.) supports B — annotating fields is closer to what an inspector wants.\n- A is consistent with what's already designed (cc-syy decision step).\n- The reviewer-action UI question is downstream of cc-o1k landing (extracted values visible) — only after that does 'I accept this drift' have a target to act on.\n\nResolution path: Tower drafts a 1-page UC-001 amendment proposing one or the other based on assignment re-read; Chair confirms; if B, derive concrete bead(s) for the persistence + UI work.\n\nThis bead is intentionally a scope-decision placeholder, not implementation. Closes when Tower+Chair land on A or B and (if B) at least one concrete follow-up bead exists.","notes":"Resolved 2026-05-02 by Tower: OPTION A — label-level decision only.\n\nRationale: (1) consistent with cc-syy as already designed; (2) matches the COLA reviewer flow described in the assignment (one label = one COLA action); (3) per-finding decisions add UI state, persistence, and decision-derivation complexity beyond what the take-home time budget can absorb without compromising other Tier 1 work; (4) the reviewer can evaluate the value of the prototype from the rule engine + transparency layer alone — per-finding overrides are interesting future work but not what makes this demo land.\n\nIf a hiring discussion later wants Option B, it cleanly slots onto today's foundation: cc-o1k's extracted_value column gives per-finding state somewhere to attach reviewer overrides; cc-syy's label-level decision becomes the final aggregator. Document this as future work in cc-64v's README known-limitations section.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-03T02:48:16.251092Z","created_by":"gjw","updated_at":"2026-05-03T02:57:21.052111Z","closed_at":"2026-05-03T02:57:21.051755Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["reevaluate"]}
{"id":"cc-scz","title":"Repo scaffolding: package.json, tsconfig, .node-version, .gitignore, docker-compose","description":"Set up TS/Node 24 project skeleton per ARCHITECTURE.md: package.json with dev/build/migrate scripts, tsconfig.json (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, useUnknownInCatchVariables, noFallthroughCasesInSwitch), .node-version pinned to 24.x for nodenv, populate currently-empty .gitignore (node_modules, dist, .env, data/incoming/*, data/processing/*, data/processed/*, data/failed/*, with .gitkeep files for the data/ subdirs), docker-compose.yml for Postgres only, .env.example. SQL access: Kysely + pg driver. Add a migration runner script (npm run migrate) using Kysely's Migrator API pointing at src/db/migrations/. Do NOT write any migrations or src/db/schema.ts content yet — that lands in cc-xog.","design":"Consumes: ARCHITECTURE.md stack list. Produces: working npm install + npm run typecheck + docker compose up -d db. Files: package.json, tsconfig.json, .node-version, .gitignore (replaces empty file), docker-compose.yml, .env.example, src/db/kysely.ts (connection factory only — no schema yet), scripts/migrate.ts (Kysely Migrator runner), data/{incoming,processing,processed,failed}/.gitkeep. Dependencies: kysely, pg, @types/pg, typescript, @types/node, tsx (for running TS scripts). Dev: vitest, eslint, @typescript-eslint/*. Do NOT scaffold React/Vite/Express here — that's cc-xog.","acceptance_criteria":"1. npm install completes clean. 2. npm run typecheck passes on empty src/. 3. docker compose up -d db starts Postgres. 4. .gitignore prevents data/ contents and .env from being tracked. 5. .node-version reads 24.x. 6. tsconfig has all 5 required strict flags. 7. scripts/migrate.ts compiles (no migrations to run yet).","status":"closed","priority":0,"issue_type":"task","assignee":"gjw","created_at":"2026-04-29T18:37:21.277711Z","created_by":"gjw","updated_at":"2026-04-29T22:14:39.987138Z","closed_at":"2026-04-29T22:14:39.986974Z","close_reason":"Repo scaffolding: package.json, tsconfig (strict flags), .node-version, .gitignore, docker-compose for Postgres, .env.example, src/db/kysely.ts factories, scripts/migrate.ts (Kysely Migrator).","source_repo":".","compaction_level":0,"original_size":0,"labels":["scaffolding"],"dependencies":[{"issue_id":"cc-scz","depends_on_id":"cc-ff6","type":"blocks","created_at":"2026-04-29T18:42:14.403418Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-srj","title":"Per-field needs_application_data on partial JSON (UC-001.4e)","description":"applicationDataSchema currently requires every field; if the JSON omits any required field, Zod throws and processStem catches the error → job to lifecycle 'failed'. UC-001.4e wants per-field reporting: comparable fields with values get compared normally, missing fields get marked needs_application_data. Loosen the Zod schema to make all fields optional, then in compareString/compareBrand/compareAbv detect missing application values and emit needs_application_data per field.","design":"src/core/schemas.ts:\n- applicationDataSchema → make brandName, classType, alcoholContent, netContents, producerName, producerAddress all optional (countryOfOrigin already is). The schema is now an interface for what the JSON CAN have, not a strict contract for what it MUST have.\n\nsrc/worker/process.ts:\n- loadApplication keeps Zod parse for type safety; failure to parse remains a hard fail (malformed JSON). But a JSON that simply omits a field is now valid input.\n\nsrc/core/verification.ts:\n- compareBrand, compareString, compareAbv: each takes the application value as 'string | undefined' (or 'number | undefined' for ABV). When undefined, return l2(fieldName, 'needs_application_data', 'Field not provided in application data.').\n- runLayer2: already short-circuits when application is null; no change there.\n\nUC-001.4e wants this. Today: dropping a JSON missing 'producerAddress' → applicationDataSchema.parse throws → job fails entirely. After: the job processes, producerAddress shows needs_application_data, every other field is compared normally.","acceptance_criteria":"1. JSON missing one field (e.g. omit producerAddress) → job lifecycle is 'processed' (not 'failed'); Layer 2 producerAddress shows needs_application_data; all other Layer 2 fields are compared normally.\n2. JSON missing multiple fields → each missing field shows needs_application_data; present fields are compared.\n3. Wholly malformed JSON (not valid JSON) → still fails the job per existing behavior.\n4. verification.test.ts adds at least one case for partial application data; npm run test passes.","status":"open","priority":0,"issue_type":"task","created_at":"2026-05-01T23:52:35.212543Z","created_by":"gjw","updated_at":"2026-05-01T23:53:36.756457Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-srj","depends_on_id":"cc-xog","type":"blocks","created_at":"2026-05-01T23:53:36.704474Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-syy","title":"Decision step (UC-001 step 6): approve / reject / send-back UI + decisions table + queue removal","description":"UC-001 step 6 is missing entirely. The agent can view findings but has no way to record a compliance decision; nothing leaves the active queue. This is the biggest gap between the prototype and 'an agent can use this to do their job.' Add an approve / reject / send_back action set on the job detail view; persist the decision with optional note; the just-decided job no longer appears in the default 'active' queue but remains accessible for inspection.","design":"DATA MODEL:\n- New table 'decisions': id, job_id (fk), outcome ('approve'|'reject'|'send_back'), note (text, optional), decided_at, decided_by (string, default 'agent' until auth is real). One decision per job for now; if a future requirement asks for revisable decisions, add a chain. New migration src/db/migrations/00X_decisions.ts.\n- jobs table gains a derived 'has_decision' or similar; or query-time join. Either is fine.\n\nAPI:\n- POST /api/jobs/:id/decision { outcome, note? } → creates row in decisions, returns updated job + decision.\n- GET /api/jobs?lifecycle=... should default-exclude jobs that have a decision (so the active queue narrows on decide). Add an explicit ?decided=true filter to make decided jobs visible for inspection.\n- GET /api/jobs/:id should include the decision if any.\n\nUI:\n- JobDetailView: decision controls at the bottom — three buttons (Approve / Reject / Send back) + an optional note textarea. On submit, POST to the API, then refresh the detail and pop back to the queue.\n- Decided jobs disappear from the default queue. Add a 'Decided' filter button alongside the existing lifecycle filters so the agent can see decided jobs.\n- VerdictBadge for the decision (green / red / amber).\n- Decision is final for the prototype — no edit/undo. Document this as a deliberate simplification in README.\n\nOUT OF SCOPE: per-field rejection reasons attached to the decision (UC-001 open question; deferred). Auth / actual agent identity (out of scope per UC-001 / REQ-007). Push notifications when a flagged item later resolves.","acceptance_criteria":"1. New migration adds decisions table with the schema above; npm run migrate succeeds.\n2. POST /api/jobs/:id/decision with { outcome: 'approve' } persists and returns 200.\n3. After decision, GET /api/jobs (default) does not include the decided job; GET /api/jobs?decided=true does.\n4. JobDetailView shows three decision buttons + optional note; clicking Approve / Reject / Send back persists and pops back to queue.\n5. The decided job's badge in the list view (when surfaced via the Decided filter) shows the outcome.\n6. typecheck/lint/test all pass; at minimum a unit test for the API endpoint covers each of the three outcomes.","status":"open","priority":0,"issue_type":"task","created_at":"2026-05-01T23:52:23.280652Z","created_by":"gjw","updated_at":"2026-05-01T23:53:07.084260Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-syy","depends_on_id":"cc-xog","type":"blocks","created_at":"2026-05-01T23:53:07.038556Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-uhc","title":"Worker re-extracts via Gemini on JSON-arrives-after-image; cache the first extraction","description":"Observed during cc-7w9 live testing: when image arrives before JSON, the worker calls Gemini twice — once during the awaiting_application step (computes Layer 1 + a needs_application_data Layer 2) and again when JSON arrives (recomputes Layer 2 against the application).\n\nPer the latest test run, this doubled the cost for cointreau and rumble pairs (image first → 2 calls each) while agave (JSON first → 1 call) cost ~half. ARCHITECTURE.md already calls out that 'Re-running Layer 2 is safe: Layer 1 results are derived from the image, and the image does not change' — but in this case we are also re-running the EXTRACTION, which is the expensive part. Layer 2 only needs the existing extracted fields plus the application JSON.\n\nSuggested approach: persist the raw ExtractedFields alongside the job (or alongside Layer 1 results) so the second processStem branch can re-run runLayer2 against the cached extraction without calling the provider again. This makes JSON-after-image arrival cheaper and matches the spirit of the architecture's 'Layer 1 stable, Layer 2 re-runnable' note.\n\nOut of scope: changing watcher event semantics or arrival-order handling. Just the cache.","status":"open","priority":2,"issue_type":"chore","created_at":"2026-05-02T03:13:17.744922Z","created_by":"gjw","updated_at":"2026-05-02T03:13:17.744922Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"cc-vho","title":"Render label image in detail view","description":"UC-001 steps 2, 5, 3b, 3c, 4b, 4c all assume the agent can see the label image alongside the findings. Currently JobDetailView shows only the verdicts; no image. Static-serve files from data/incoming/ (or wherever the watcher leaves them post-pairing) via Express; render <img> in JobDetailView with the application JSON shown adjacent for context.","design":"BACKEND:\n- src/server/index.ts adds an Express static-serve mount for the directory where the watcher leaves files (data/incoming/ for now, or data/processed/ if a post-processing move is added later — confirm by reading watcher.ts and process.ts). Mount path: /api/files/<stem>.<ext>. Restrict to image extensions (.jpg/.jpeg/.png/.webp) to avoid serving arbitrary files.\n- jobs API response already includes image_path. Add a derived url field (e.g. '/api/files/cointreau.jpg') so the UI does not need to know the on-disk layout.\n\nUI:\n- JobDetailView gains an image panel above or beside the findings sections. Use the URL from the API response. Image scales to viewport width with max-height for readability; click to open full-size in new tab.\n- If application_path exists, render the parsed application JSON in a small panel adjacent to the image, formatted as a key/value table (so the agent can do their own visual cross-check independent of Layer 2's machine output).\n\nOUT OF SCOPE: auth on the static endpoint (consistent with REQ-007 / no auth in prototype). Image preprocessing or thumbnail generation; serve the original composite directly.","acceptance_criteria":"1. GET /api/files/<existing-fixture-stem>.jpg returns the image with appropriate content-type.\n2. GET /api/files/<stem>.exe (or any non-image extension) returns 404 or 403.\n3. JobDetailView renders the label image when image_path is set on the job; falls back to a 'no label image' placeholder when not.\n4. Application JSON values are visible adjacent to the image as a key/value panel.\n5. typecheck/lint/test pass.","notes":"Landed in c4aa5dc, merged to main. Express static-serve at /api/files/<stem>.<ext> restricted to image extensions; JobDetailView renders the label image and application data alongside the findings sections. Acceptance 1-5 verified; typecheck/lint/test pass.","status":"closed","priority":0,"issue_type":"task","assignee":"gjw","created_at":"2026-05-01T23:52:26.711177Z","created_by":"gjw","updated_at":"2026-05-03T02:56:59.315891Z","closed_at":"2026-05-03T02:56:59.315473Z","close_reason":"Added /api/files static-serve route (extension whitelist, traversal-safe, 200/403/404/400 paths covered by integration tests), image_url derived field on jobs, application data parsed and returned in detail response, JobDetailView gains LabelImagePanel + ApplicationPanel in a 1fr-320px grid above findings.","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-vho","depends_on_id":"cc-xog","type":"blocks","created_at":"2026-05-01T23:53:17.547085Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-wvs","title":"Add extraction_confidence (low/med/hi) to Layer 1 result schema","description":"Before cc-xog lands the vertical slice, define an extraction_confidence enum (low | med | hi) on the Layer 1 per-field result so the UI can distinguish 'the label says X and X is wrong' from 'we think the label says X but the image was hard to read.' This is per-field extraction confidence, not a verdict-level score — verdicts remain pass / fail / needs_review per REQ-006. Source: F-039 (bad angles, glare, lighting) makes low-confidence reads a real category we have to surface; without an explicit field, REQ-009 (handle imperfect images gracefully) has nothing to hang on. Mapping rule: derive from whatever Gemini 3.1 Pro returns for per-field extraction (logprob band or self-rated score) into three buckets; record the raw value alongside the bucket for future tuning. UI hint: render as a small badge on the field row, not a numeric column. Acceptance: (1) layer1_results table has extraction_confidence column per field result, (2) stub provider returns plausible buckets on fixtures, (3) UI shows a 'low-confidence read' indicator when bucket=low, (4) needs_review verdict still drives the queue, not the confidence bucket alone.","status":"closed","priority":1,"issue_type":"decision","created_at":"2026-04-29T22:34:09.708351Z","created_by":"gjw","updated_at":"2026-04-30T02:40:29.852107Z","closed_at":"2026-04-30T02:40:29.851688Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"cc-xog","title":"Vertical slice: ingestion + worker + stub verifier + review UI","description":"First end-to-end pass: Express server, Chokidar watcher (subscribed to .jpg/.jpeg/.png/.webp AND .json with awaitWriteFinish enabled), worker entry point, Kysely migrations for jobs/layer1_results/layer2_results tables, a stubbed analyzeLabel provider returning fixed ExtractedFields, a regulation-aware verifyLabel (in core/verification.ts) implementing the actual TTB rules from F-047/F-048/F-052 against the stubbed extracted fields (no real model call yet), and a queue-first React+Vite review UI. The watcher pairs files by filename stem per F-046/REQ-012: upsert job by stem; image-first creates job, runs Layer 1, then transitions to lifecycle awaiting_application until JSON arrives, at which point Layer 2 runs without re-running Layer 1; json-first creates job in lifecycle awaiting_label with no layer runs; later arrival of the missing sibling drives the appropriate transitions. Browser upload writes through to data/incoming/ so the upload route and the watcher share one ingestion code path. Goal: a label dropped in data/incoming/ ends up reviewable in the UI within seconds. Deps: cc-scz scaffolding task (closed).","design":"STATE MODEL (split axes per Chair decision 2026-04-29). Lifecycle (single column on jobs): awaiting_label | awaiting_application | queued | processing | processed | failed. Verdict (per-field on result rows): Layer 1 fields take pass | fail | needs_review; Layer 2 fields take pass | fail | needs_review | needs_application_data. failed is reserved for system-level failures (worker crash, file-read error); a label whose Layer 1 fields all came back needs_review is processed with bad verdicts, not failed. The verdict-derived 'needs review' UI bucket is a filter over processed jobs whose result rows contain any needs_review.\n\nLAYER 1 SCHEMA (per F-056/REQ-009): each layer1 per-field result row carries an extraction_confidence column with values low | med | hi, derived from whatever signal the extraction model returns (logprobs or self-rated score), bucketized. Store the raw value alongside the bucket for future tuning. The UI renders extraction_confidence as a per-field badge, not a column or numeric score, and never as part of the verdict surface.\n\nCONSUMES: cc-scz scaffolding (package.json, tsconfig, Kysely + pg, docker-compose, data/ dirs). PRODUCES: working ingestion -> Layer 1/2 verifier -> review UI.\n\nKEY FILES (planning, may shift): src/server/index.ts, src/server/routes/jobs.ts (list, get), src/server/routes/upload.ts (multipart label+json; writes through to data/incoming/, never DB-direct), src/worker/index.ts, src/worker/watcher.ts (chokidar subscribed to image+json globs, awaitWriteFinish on, upsert-by-stem), src/worker/pair.ts (stem matching, sibling lookup, lifecycle transitions), src/core/providers/types.ts (LabelProvider interface with analyzeLabel), src/core/providers/stub.ts (stub returning fixed ExtractedFields with plausible per-field extraction_confidence), src/core/verification.ts (verifyLabel({extracted, application}) returning {layer1, layer2}; Layer 1 implements F-047 warning exact-text, F-048 GOVERNMENT WARNING caps+bold check on extracted text, F-052 wine ABV format/tolerance rule; Layer 2 implements F-051/F-053/F-054 mandatory-field comparison plus brand-name tolerant matching per F-030), src/core/schemas.ts (Zod for ApplicationData + provider boundary), src/db/schema.ts (Kysely Database interface for jobs/layer1_results/layer2_results), src/db/migrations/001_init.ts, src/web/* (Vite React SPA: queue list view filtered by lifecycle and verdict-rollup, single job detail showing Layer 1 and Layer 2 results separately with extraction_confidence badges per Layer 1 field).\n\nApplicationData schema per F-046: brandName, classType, alcoholContent, netContents, producerName, producerAddress, countryOfOrigin?. Government warning is NOT in ApplicationData (regulation-defined; see F-046 and F-047).\n\nSTUB BOUNDARY: analyzeLabel (the model boundary) is stubbed and returns canned ExtractedFields with plausible extraction_confidence buckets. verifyLabel (the regulatory logic) is REAL — it actually compares the canned extracted fields to the regulation (F-047/F-048/F-052) and to ApplicationData (F-051/F-053/F-054), and produces honest per-field verdicts. This means cc-htd fixtures will produce real verdicts as soon as they exist.\n\nOUT OF SCOPE FOR THIS SLICE (per F-057, REQ-012 out_of_scope_facts): same-stem replacement of an already-ingested file. Dropping a new file with the same stem after a layer has run does not re-version inputs or re-run affected layers.","acceptance_criteria":"1. Drop a paired image+JSON into data/incoming/ -> a job appears in the UI within seconds with Layer 1 and Layer 2 per-field results populated; lifecycle transitions queued -> processing -> processed.\n2. Drop only an image -> job appears with Layer 1 per-field results (each carrying an extraction_confidence badge) and Layer 2 fields all marked needs_application_data; lifecycle transitions queued -> processing -> awaiting_application.\n3. Drop only a JSON -> job appears in lifecycle awaiting_label with no Layer results.\n4. Drop the missing sibling later -> existing job is updated (no duplicate created); in the image-first case, lifecycle transitions awaiting_application -> queued -> processing -> processed and Layer 2 re-runs without re-running Layer 1; in the JSON-first case, lifecycle transitions awaiting_label -> queued -> processing -> processed and both layers run.\n5. Browser upload of a paired or label-only submission writes the files to data/incoming/ and produces the same internal representation as a watched-directory drop (the watcher is the single ingestion code path).\n6. UI distinguishes Layer 1 (well-formedness) vs Layer 2 (comparison) results visually, and renders extraction_confidence as a per-field badge on Layer 1 fields (not a column or numeric score).\n7. npm run typecheck, lint, test all pass on the resulting code; pair.ts has unit tests covering all three arrival cases plus the lifecycle transitions in (1)-(4).\n8. No real Gemini call yet — analyzeLabel is stubbed. verifyLabel implements real TTB rules (F-047/F-048/F-052/F-051/F-053/F-054) against the stubbed extracted fields, so the prototype produces honest per-field verdicts even before the model boundary is wired up.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-04-29T18:37:21.324178Z","created_by":"gjw","updated_at":"2026-04-30T12:47:43.181073Z","closed_at":"2026-04-30T12:47:43.180633Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-xog","depends_on_id":"cc-scz","type":"blocks","created_at":"2026-04-29T18:37:28.313981Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
{"id":"cc-ye9","title":"Decouple low-confidence from Layer 1 verdict (UC-001.3c)","description":"checkRequiredField currently flips verdict to needs_review when extractionConfidence is low, regardless of whether the rule itself passed. UC-001.3c explicitly wants confidence and verdict to be separable signals so the agent can tell 'label says X and X is wrong' apart from 'we think label says X but image was hard to read.' Layer 1 verdict should be the rule outcome only; confidence remains the per-field badge. Affects checkRequiredField, checkAbvFormat, and any other Layer 1 step that conflates the two.","design":"src/core/verification.ts:\n- checkRequiredField: drop the 'if confidence === low return needs_review' branch. Verdict reflects the rule outcome only (mandatory field present? pass; empty? fail). Keep extractionConfidence on the result row so the UI badge still shows it.\n- checkAbvFormat: same — remove the low-confidence override that flips parsed-OK ABV to needs_review. Verdict is pass when format parses; the low-confidence badge alongside is the agent's signal that they should re-confirm by eye.\n- checkWarning: already does not branch on confidence; leave as-is.\n\nUI does not need changes — JobDetailView already renders the badge and verdict separately. The fix is purely in verification.ts.\n\nUpdate src/core/verification.test.ts cases that asserted the conflated behavior; replace with the decoupled expectations.","acceptance_criteria":"1. A field with verdict-pass-on-rule-but-low-confidence shows verdict 'pass' and badge 'low' in the UI (not verdict 'needs_review').\n2. A field with verdict-fail-on-rule-and-low-confidence shows verdict 'fail' and badge 'low'.\n3. verification.test.ts updated; npm run test passes.\n4. UC-001.3c semantics hold: agent can distinguish 'label says X and X is wrong' from 'we think label says X but image was hard to read' by reading verdict and badge separately.","status":"open","priority":0,"issue_type":"task","created_at":"2026-05-01T23:52:31.400585Z","created_by":"gjw","updated_at":"2026-05-01T23:53:26.327609Z","source_repo":".","compaction_level":0,"original_size":0,"labels":["mvp"],"dependencies":[{"issue_id":"cc-ye9","depends_on_id":"cc-xog","type":"blocks","created_at":"2026-05-01T23:53:26.275388Z","created_by":"gjw","metadata":"{}","thread_id":""}]}
